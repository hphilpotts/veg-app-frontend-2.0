import React from 'react';

import { Container } from '@mui/material';

export const CategoryButton = ({ containerStyle }) => {
    return (
        <Container sx={containerStyle}>
            <p>Category Button</p>
        </Container>
    );
};